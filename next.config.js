/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  images: {
    // this will accept any pics that come from this link 
    domains: ['links.papareact.com'],
  }
}
